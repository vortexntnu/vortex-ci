coverage:
  precision: 2
  round: down
  range: "70...100"

comment:
  layout: "reach, diff, flags, files"
  behavior: default

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

ignore:
  - "tests/*"
  - "examples/*"

fixes:
  - "::src/"
  - "vortex_ws/src/*/vortex-auv/::"

flags:
  unittests:
    paths:
      - "tests/"
  integration:
    paths:
      - "tests/integration/"